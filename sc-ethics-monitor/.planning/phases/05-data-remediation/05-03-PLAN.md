---
phase: 05-data-remediation
plan: 03
type: standard
---

# Plan 05-03: Populate Incumbent Data

<objective>
Populate incumbent_name and incumbent_party columns in Race Analysis and Districts tabs.

Purpose: party-data.json contains complete incumbent data (124 House + 46 Senate = 170 total) but this data is not appearing in the Google Sheet.

Output: All 170 district rows have incumbent_name and incumbent_party populated.
</objective>

<context>
@.planning/phases/05-data-remediation/DESIGN.md
@src/data/party-data.json
@src/sheets_sync.py
</context>

<tasks>

<task type="auto" id="1">
  <name>Verify party-data.json incumbent structure</name>
  <action>
    Read `src/data/party-data.json` and verify:
    - incumbents.house has entries for districts 1-124
    - incumbents.senate has entries for districts 1-46
    - Each entry has: name, party
    - Sample format verification

    Document the exact JSON path structure.
  </action>
  <verify>python -c "import json; d=json.load(open('src/data/party-data.json')); print(len(d['incumbents']['house']), len(d['incumbents']['senate']))"</verify>
  <done>Confirmed 124 House + 46 Senate incumbents in party-data.json</done>
</task>

<task type="auto" id="2">
  <name>Create populate_incumbents.py script</name>
  <action>
    Create `scripts/populate_incumbents.py`:
    1. Load party-data.json
    2. Connect to Google Sheets
    3. For each incumbent in house/senate:
       - Build district_id (e.g., "SC-House-042")
       - Find matching row in Race Analysis
       - Update incumbent_name and incumbent_party columns
    4. Log updates made
    5. Handle missing rows gracefully
  </action>
  <verify>Script runs without errors on dry-run mode</verify>
  <done>populate_incumbents.py script created and functional</done>
</task>

<task type="auto" id="3">
  <name>Determine column positions for incumbent data</name>
  <action>
    Check Google Sheet Race Analysis tab:
    - Which column is incumbent_name? (likely column E or F)
    - Which column is incumbent_party? (likely column F or G)
    - Are these columns already present or need to be added?

    Document exact column letters.
  </action>
  <verify>Column positions documented</verify>
  <done>Know exact columns to update</done>
</task>

<task type="auto" id="4">
  <name>Run incumbent population script</name>
  <action>
    Execute the script:
    ```bash
    cd /Users/russellteter/Desktop/sc-election-map-2026/sc-ethics-monitor
    python scripts/populate_incumbents.py
    ```

    Monitor output for any errors or skipped districts.
  </action>
  <verify>Script completes with 170 updates logged</verify>
  <done>All 170 districts have incumbent data populated</done>
</task>

<task type="auto" id="5">
  <name>Update Districts tab if needed</name>
  <action>
    Check if Districts tab also needs incumbent data:
    - Does it have incumbent_name column?
    - Does it have incumbent_party column?

    If yes, update the script to populate both tabs.
    If no, document that Race Analysis is the authoritative source.
  </action>
  <verify>Districts tab checked and updated if applicable</verify>
  <done>Both tabs have consistent incumbent data</done>
</task>

<task type="auto" id="6">
  <name>Verify incumbent data accuracy</name>
  <action>
    Sample verification:
    1. Pick 10 random districts (5 House, 5 Senate)
    2. Verify incumbent_name matches party-data.json
    3. Verify incumbent_party matches (Republican/Democrat format)
    4. Verify district mapping is correct (no off-by-one errors)

    Document any discrepancies.
  </action>
  <verify>All 10 sample districts have correct incumbent data</verify>
  <done>Incumbent data verified accurate</done>
</task>

<task type="auto" id="7">
  <name>Add incumbent population to update workflow</name>
  <action>
    Ensure incumbent data stays populated:
    - Add incumbent population to sheets_sync.py if not present
    - Or document that it should be run manually after party-data.json updates

    Document the maintenance procedure.
  </action>
  <verify>Procedure documented for keeping incumbent data current</verify>
  <done>Maintenance procedure established</done>
</task>

</tasks>

<verification>
- [ ] party-data.json has 170 incumbents (124 + 46)
- [ ] populate_incumbents.py script created
- [ ] Column positions documented
- [ ] Script executed successfully
- [ ] Race Analysis tab has incumbent_name for all 170 rows
- [ ] Race Analysis tab has incumbent_party for all 170 rows
- [ ] Sample verification passes (10 districts)
- [ ] Maintenance procedure documented
</verification>

<success_criteria>
- All 170 districts have incumbent_name populated
- All 170 districts have incumbent_party populated (R or D)
- Data matches party-data.json source
- No blank incumbent fields for districts with incumbents
</success_criteria>

<output>
Create 05-03-SUMMARY.md documenting:
- populate_incumbents.py script location and usage
- Incumbent statistics:
  - House: 124 incumbents (R count, D count)
  - Senate: 46 incumbents (R count, D count)
- Any open seats (no incumbent)
- Maintenance procedure for future updates
</output>
